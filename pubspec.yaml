name: tracer_x
description: >
  Execution trace recording and golden-master Fix Receipts for Dart —
  capture ordered state checkpoints, persist .tracer.json, and diff buggy vs fixed flows.
version: 0.2.2
repository: https://github.com/lukeschleder/tracerX
homepage: https://github.com/lukeschleder/tracerX
issue_tracker: https://github.com/lukeschleder/tracerX/issues

environment:
  sdk: ^3.0.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0

topics:
  - logging
  - tracing
  - debugging
  - testing
