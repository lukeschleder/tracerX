name: tracer_x
description: Execution trace recording, golden-master diffing, and pluggable logging sinks for Dart.
version: 0.2.0
repository: https://github.com/example/tracer_x

environment:
  sdk: ^3.0.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
